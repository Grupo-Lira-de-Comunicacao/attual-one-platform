"use client";
import { useState } from "react";
import { Building2, ChevronRight } from "lucide-react";
import type { CompanyMembership } from "@/lib/supabase/session";
export function CompanySelector({ memberships }: { memberships: CompanyMembership[] }) { const [loading, setLoading] = useState(""); const [error, setError] = useState(""); const select = async (companyId: string) => { setLoading(companyId); setError(""); try { const response = await fetch("/api/company/select", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ companyId }) }); const result = await response.json() as { error?: string }; if (!response.ok) throw new Error(result.error ?? "Não foi possível selecionar."); window.location.assign("/"); } catch (reason) { setError(reason instanceof Error ? reason.message : "Falha de rede."); setLoading(""); } }; return <main className="auth-page"><section className="auth-panel"><p className="eyebrow">MULTIEMPRESA</p><h1>Selecione a empresa</h1><p>Escolha a operação que deseja acessar.</p><div className="company-options">{memberships.map((membership) => <button key={membership.companyId} disabled={Boolean(loading)} onClick={() => select(membership.companyId)}><Building2 /><span><strong>{membership.companyName}</strong><small>{membership.role}</small></span><ChevronRight /></button>)}</div>{error && <div className="auth-message">{error}</div>}</section></main>; }
