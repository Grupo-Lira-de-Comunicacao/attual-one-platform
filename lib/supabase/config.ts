export type DataMode = "local" | "supabase";
export function getSupabasePublicConfig(env: NodeJS.ProcessEnv = process.env) { const mode: DataMode = env.NEXT_PUBLIC_DATA_MODE === "supabase" ? "supabase" : "local"; const url = env.NEXT_PUBLIC_SUPABASE_URL?.trim() ?? ""; const publishableKey = env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY?.trim() ?? ""; return { mode, url, publishableKey, configured: Boolean(url && publishableKey) }; }
export function requireSupabasePublicConfig(env: NodeJS.ProcessEnv = process.env) { const config = getSupabasePublicConfig(env); if (config.mode !== "supabase") throw new Error("O modo Supabase não está ativado."); if (!config.configured) throw new Error("Configure NEXT_PUBLIC_SUPABASE_URL e NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY para usar o Supabase."); return config; }
