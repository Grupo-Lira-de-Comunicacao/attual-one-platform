export type DataMode = "local" | "supabase";
export function getSupabasePublicConfig() { const mode: DataMode = process.env.NEXT_PUBLIC_DATA_MODE === "supabase" ? "supabase" : "local"; const url = process.env.NEXT_PUBLIC_SUPABASE_URL?.trim() ?? ""; const publishableKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY?.trim() ?? ""; return { mode, url, publishableKey, configured: Boolean(url && publishableKey) }; }
export function requireSupabasePublicConfig() { const config = getSupabasePublicConfig(); if (config.mode !== "supabase") throw new Error("O modo Supabase não está ativado."); if (!config.configured) throw new Error("Configure NEXT_PUBLIC_SUPABASE_URL e NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY para usar o Supabase."); return config; }
