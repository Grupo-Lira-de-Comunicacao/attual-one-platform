import test from"node:test";import assert from"node:assert/strict";import{buildCsv,csvCell,csvRow}from"../lib/csv.ts";
test("csvCell escapa aspas duplas e sempre entre aspas",()=>{assert.equal(csvCell("simples"),'"simples"');assert.equal(csvCell('Vendas com "aspas"'),'"Vendas com ""aspas"""');assert.equal(csvCell(10.5),'"10.5"')});
test("csvRow junta células com ponto e vírgula",()=>{assert.equal(csvRow(["a","b",1]),'"a";"b";"1"')});
test("buildCsv monta seções com título, cabeçalho e linhas, separadas por linha em branco",()=>{const csv=buildCsv([{title:"Resumo",headers:["Indicador","Valor"],rows:[["Vendas",10.5]]},{title:"Produtos",headers:["Produto","Receita"],rows:[["Smash Bacon",44.9],['Combo "Família"',149.9]]}]);const lines=csv.split("\n");assert.equal(lines[0],"Resumo");assert.equal(lines[1],'"Indicador";"Valor"');assert.equal(lines[2],'"Vendas";"10.5"');assert.equal(lines[3],"");assert.equal(lines[4],"Produtos");assert.match(csv,/"Combo ""Família""";"149\.9"/)});
